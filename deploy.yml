name: Continuous Deployment to OCI

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del codice
        uses: actions/checkout@v4

      - name: Log di inizio build
        run: echo "[LOG] Avvio della compilazione dell'immagine Docker per OCI Container Instances..."

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build e Push dell'immagine
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/opendeep-ai:latest

      - name: Log di completamento
        run: echo "[LOG] Immagine caricata con successo su Docker Hub. Pronta per il pull su OCI Container Instances."